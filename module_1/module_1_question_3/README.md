## Question 3

Create uv environment:

`uv init -p 3.11 --name module_1_question_3`

Add required dependencies:

`uv add pandas pyarrow`

Add Jupyter:

`uv add --dev jupyter`

Run jupyter notebook:

`uv run jupyter notebook`


